# **習慣育成アプリ 統合仕様書 v1.1**

## **1\. 統合方針**

自己利用を前提とし、厳格な習慣形成とペナルティ（罰）を許容する方針で統一した。

> * Flutter / Dart / Xcodeを前提とする  
> * 初期リリースはiOSを優先する  
> * ローカルファーストで、サーバー・アカウント・課金・広告は持たない  
> * 状態管理はRiverpod、DBはDrift/SQLiteを使用する  
> * 1習慣につき1体の生き物を育成する  
> * 同時利用できるアクティブ習慣は最大3件  
> * 1体につき3段階の進化を持つ  
> * 成長履歴を折れ線チャートで表示する  
> * 最低ライン達成と通常ライン達成を区別して記録する  
> * **通常ライン達成時はN(10, 3)の正規分布（下限7）に従って増加する**  
> * **最低ライン達成時はN(5, 2)の正規分布（下限3）に従って増加する**  
> * **未達成時は現在ポイントから X \~ N(10, 3\) の正規分布に従い、-X%の割合で減少する**  
> * **未達成日はチャートが下降する**  
> * **累積ポイントが閾値を下回った場合は進化段階が下がる（退化する）**  
> * 運用日はホームタイムゾーンと午前4時境界で判定する  
> * ホームタイムゾーンと日付境界は最初の習慣作成後に固定する  
> * 完全バックアップの外部書き出し・復元を正式機能に含める

## **2\. アプリ名候補**

| 候補 | 読み | 特徴 |
| :---- | :---- | :---- |
| のびログ | のびログ | 成長と記録が分かりやすく、金融アプリと誤認されにくい |
| そだチャート | そだチャート | 育成とチャートという差別化要素を直接表現できる |
| コツコツモン | コツコツモン | 習慣継続と生き物育成の親しみやすさを重視 |
| つづくる | つづくる | 「続く」と「つくる」を合わせたブランド型名称 |
| ソダチセン | ソダチセン | 習慣の成長が一本の線になるというコンセプト |

本仕様では仮称を**「のびログ」**とする。  
公開前に以下を確認する。

> * 商標登録  
> * App Store内の同名・類似名  
> * ドメイン名  
> * SNSアカウント名  
> * 日本語・英語圏での不適切な意味

## **3\. 製品仕様**

### **3.1 中心体験**

> * 習慣名、最低ライン、通常ライン、実施曜日を登録する  
> * 実施日に「最低ラインできた」または「通常ラインできた」を押す  
> * 通常ライン達成でN(10, 3)\[下限7\]、最低ライン達成でN(5, 2)\[下限3\]のポイントが成長する  
> * 未達成の場合は N(10, 3)% の割合で累積ポイントが減少する  
> * 成長・減少がチャートに反映される  
> * 累積ポイントに応じて生き物が進化、または退化する

### **3.2 初期版の範囲**

#### **含める機能**

> * オンボーディング  
> * 習慣作成・編集  
> * 最大3件のアクティブ習慣  
> * 最低ライン・通常ライン記録、未達成ペナルティ処理  
> * 休息日  
> * 休止・再開  
> * アーカイブ  
> * 3段階の生き物進化・退化  
> * 7日・30日・90日・365日チャート  
> * 履歴・統計  
> * ローカル通知  
> * JSONバックアップ・復元  
> * 履歴CSV出力  
> * VoiceOver、Dynamic Type、ダークモード  
> * 「動きを減らす」設定

#### **含めない機能**

> * 課金・広告  
> * ログイン・サーバー同期  
> * SNS、ランキング、フレンド  
> * HealthKit  
> * AIによる健康・心理評価  
> * ウィジェット  
> * Android向け最適化  
> * 自動クラウド同期  
> * 任意の古い日付への達成追加  
> * 株価・金融データとの連携

## **4\. 技術構成**

| 項目 | 採用方針 |
| :---- | :---- |
| UI | Flutter |
| 言語 | Dart |
| iOSビルド | Xcode |
| 最低対応OS | iOS 16を候補とし、実装開始時に確定 |
| 状態管理・DI | Riverpod |
| DB | Drift \+ SQLite |
| チャート | fl\_chart |
| 通知 | flutter\_local\_notifications |
| タイムゾーン | timezone \+ 端末タイムゾーン取得アダプター |
| ID | UUID |
| バックアップ | JSONファイル |
| ファイル選択 | file\_selector等をDocumentGatewayで抽象化 |
| 共有 | share\_plus等をShareGatewayで抽象化 |

パッケージの具体的バージョンは、開発開始時の互換性が確認された安定版に固定し、pubspec.lockをバージョン管理する。

## **5\. ディレクトリ構成**

`lib/`  
  `app/`  
    `app.dart`  
    `bootstrap.dart`  
    `router.dart`  
    `providers.dart`

  `core/`  
    `result.dart`  
    `errors.dart`  
    `clock.dart`  
    `id_factory.dart`  
    `local_date.dart`  
    `random_generator.dart // 正規分布生成用に追加`

  `domain/`  
    `models/`  
      `habit.dart`  
      `habit_plan.dart`  
      `creature.dart`  
      `day_record.dart`  
      `growth_grant.dart`  
      `time_policy.dart`  
      `app_preferences.dart`  
    `services/`  
      `day_resolver.dart`  
      `schedule_resolver.dart`  
      `growth_engine.dart`  
      `evolution_engine.dart`  
      `statistics_calculator.dart`  
      `chart_builder.dart`  
      `clock_guard.dart`  
    `...`

## **6\. ドメインルール**

### **6.1 習慣**

> * アクティブ習慣は最大3件  
> * 習慣名は1〜40文字  
> * 最低ライン・通常ラインは1〜80文字  
> * 実施曜日は1日以上  
> * 開始日は当日または翌日  
> * 過去の計画を上書きせず、計画バージョンを保存する  
> * 目標・曜日変更は翌運用日から適用する  
> * 習慣名と生き物名は即時変更可能

### **6.2 達成記録**

`enum CompletionKind {`  
  `minimum,`  
  `standard,`  
`}`

`enum DayRecordState {`  
  `minimum,`  
  `standard,`  
  `rest,`  
  `missed, // 未達成状態を明示的に追加`  
  `cancelled,`  
`}`

> * 通常ライン達成と最低ライン達成で成長量の分布が異なる  
> * 同じ習慣・同じ運用日の有効記録は1件  
> * 記録可能日は当日と前運用日のみ  
> * 未来日への記録は禁止  
> * 過去の既存記録は取り消せるが、入力期限外では再登録できない

### **6.3 未達成**

> * 未達成行をDBへ生成し、ペナルティを記録する（GrowthGrantとして負の値を記録）  
> * 未達成時は現在の累積ポイントから X \~ N(10, 3\) の正規分布に従い、-X%の割合でポイントを減少させる  
> * 未達成日はチャートが下降する  
> * 当日の未記録は運用日終了までは「未達成」ではなく「未記録」  
> * 3回連続の未達成なら、目標見直しを提案する

### **6.4 成長**

> * 成長モードは正規分布（Normal Distribution）をベースとする  
> * 通常ライン達成時：N(10, 3)の正規分布に従う（下限7ポイント）  
> * 最低ライン達成時：N(5, 2)の正規分布に従う（下限3ポイント）  
> * 未達成時：N(10, 3)の正規分布に基づく割合(%)で減少  
> * 成長量および減少量は GrowthGrant として保存する（減少時はマイナスの値）  
> * 同じ日に再登録した場合は元の成長量を再利用せず、再計算または既存のシードに基づいて決定する

### **6.5 進化**

> * 初期表示サイズ：100  
> * 第1段階：初期状態  
> * 第2段階：累積成長ポイント70以上  
> * 第3段階：累積成長ポイント290以上  
> * 累積ポイントが閾値を下回った場合、進化段階は退化する  
> * 種族による性能差は設けない

### **6.6 休息**

> * 当日または前運用日に設定できる  
> * 成長・減少は発生しない  
> * 達成率の分母から除外する  
> * 連続達成を途切れさせない

### **6.7 運用日**

> * ホームタイムゾーンはIANA形式で保存  
> * 初期日付境界は午前4時  
> * 選択可能範囲は0〜5時

## **7\. 共通型**

`// LocalDate等の定義は既存通り`

## **8\. データモデル**

主要な変更点として GrowthGrant に負の値を許容し、減少分を記録できるようにする。

### **8.6 GrowthGrant**

`@immutable`  
`class GrowthGrant {`  
  `const GrowthGrant({`  
    `required this.id,`  
    `required this.habitId,`  
    `required this.creatureId,`  
    `required this.day,`  
    `required this.ordinal,`  
    `required this.amount, // 減少時は負の値`  
    `required this.issuedAtUtc,`  
  `});`

  `final String id;`  
  `final String habitId;`  
  `final String creatureId;`  
  `final LocalDate day;`  
  `final int ordinal;`  
  `final int amount;`  
  `final DateTime issuedAtUtc;`  
`}`

## **9\. 純粋ドメイン関数**

### **9.3 GrowthEngine**

`abstract interface class GrowthEngine {`  
  `int calculateStandardGrowth({required Uint8List seed}); // N(10,3) 下限7`  
  `int calculateMinimumGrowth({required Uint8List seed}); // N(5,2) 下限3`  
  `int calculatePenaltyPercentage({required Uint8List seed}); // N(10,3)`  
    
  `int totalPoints({`  
    `required List records,`  
    `required List grants,`  
    `LocalDate? throughDay,`  
  `});`  
`}`

## **18\. アクセシビリティ・表現**

> * 色だけで上昇・下降・状態を区別しない  
> * アイコン、線種、ラベル、符号付き数値を併用する  
> * 使用する文言例：「今日はまだ記録されていません」「目標を少し小さくしますか」「サボったので弱りました」「連続記録が途切れました」  
> * 自分用アプリとして厳しいペナルティを課すため、弱る演出や厳しい表現を許容する

## **22\. v1受け入れ条件**

> * 最大3件の習慣を登録できる  
> * 最低ライン・通常ラインを区別して記録できる  
> * 通常達成でN(10, 3)\[下限7\]、最低達成でN(5, 2)\[下限3\]の成長が行われる  
> * 未達成で累積サイズが正規分布に基づいて割合減少する  
> * 累積ポイントの増減に応じて、第2・第3段階への進化・退化が正しく行われる  
> * 境界4時の前後で運用日が正しく切り替わる  
> * 重大なデータ消失・二重成長・部分保存がない